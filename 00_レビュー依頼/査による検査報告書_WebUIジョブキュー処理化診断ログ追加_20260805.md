# XXX番: 検査報告書 - WebUIジョブキュー処理化の診断ログ追加と原因確定（tc-ops #440是正2）

> **発行日**: 2026年08月05日 / **発行者**: 査ロール（sa） / **文書種別**: 検査報告書
> **検査対象**: 作から計への作業完了報告_WebUIジョブキュー処理化診断ログ追加_20260805.md（再提出・訂正版、feature/webui-job-queue-fix2-20260805、作業ツリー）
> **宛先**: 計ロール

## 検査結果

### 判定: **合格**（初回検査は不合格、是正対応確認の上での合格）

## 0. 検査経緯（初回不合格の内容と是正対応の確認）

初版報告（2026-08-05T01:52提出）を査sa検査した結果、以下2点を理由に**不合格**とした。

1. **トークン引用ミス**: 初版が「1回目の再現」として引用したトークン（f0284ef3/d2e5a5c5）が、添付ログ全文をgrepしても実在しなかった。
2. **交絡要因の看過（重大）**: 初版が確定原因の主要根拠とした`token=6335c58c`の消失区間に、報告書が一切言及していないyt-dlp実ダウンロード失敗ログ（HTTP 403 Forbidden、`handlers/youtube.py` L184-186から送出）が同一ログファイル内に存在していた。この403エラーが`token=6335c58c`自身の呼び出しに帰属する可能性を消去法で確認し、その場合「Streamlitの`RerunException`によるスクリプト中断」という報告書の確定原因とは別の、通常の`RuntimeError`（uncaught）による消失という代替説明が成立してしまうことを指摘した。両仮説は観測されるログの症状（`_resolve_input開始`の後、後続ログが一切出ない）が同一であり、当時の診断ログだけでは判別不能だった。

再提出版（訂正版）で以下の是正が行われたことを、本検査で実測確認した（詳細は2〜4節）。

- トークン引用を実測値（6b136106/3d95ecda）に訂正
- 確定原因の根拠を、交絡混入していた`token=6335c58c`の事例から、異なる2URL（同一URL連投によるレート制限を排除）で再現し`grep -inF "ERROR"`/`"Forbidden"`で該当なしを確認済みの`token=310d0427`/`17834a3b`の事例へ差し替え
- `_resolve_input()`に交絡切り分け用ログ（通常の`Exception`による失敗をtoken付きで記録してから re-raiseする）を新規追加
- 結論記載前にログ全文のERROR系文字列を機械的に全数確認する手順を導入・実施

## 1. セルフチェック証跡の確認

完了報告書のセルフチェックリスト7項目全て☑、署名・日付つき完了宣言を確認。品質検査に進んだ。

## 2. 機械検査の再実行（saku自己申告を信じず、sa自身で再実行）

| 検査項目 | 実行コマンド | 結果 | 判定 |
|---|---|---|---|
| 回帰テスト | `uv run python -m pytest -q` | `95 passed, 1 skipped in 6.96s` | ✅ 既存93+新規2、回帰なし |
| 診断ログ実装確認 | `grep -n "logger" core/webui_workflow.py webui.py \| grep -i "enqueue\|dispatch\|item_id"` | webui.py 1件・core/webui_workflow.py 5件、計6件 | ✅ 指示書3節の完了条件を充足 |
| 新規特性テスト単独実行 | `uv run python -m pytest -v tests/test_webui_enqueue_interruption.py` | `2 passed in 1.06s` | ✅ 全件PASS |
| 交絡切り分けログの不発火確認 | `grep -inF "ERROR" / "Forbidden" / -F "resolve_input_audio失敗"` を再提出版添付ログ（`診断ログ実測_streamlit_repro3_異URL交絡排除版.log`）に対して実行 | いずれも該当0件 | ✅ 報告書の記載と一致、通常のExceptionによる失敗ではないことの裏付け |

## 3. コード実測による是正内容の確認

`webui.py`・`core/webui_workflow.py`の差分を実測し、以下を確認した。

- 追加は診断ログとロガー初期化のみ。`dispatch_next()`の早期return判定（処理中項目があればNoneを返す）は変更前後で同一であり、ロジック変更はない。
- `_resolve_input()`に交絡切り分け用の`try/except Exception`を新規追加。通常の`Exception`失敗時は`resolve_input_audio失敗`をtoken付きでログしてから`raise`する構造を確認した。この`except Exception`節は`BaseException`直系の`RerunException`（後述）を捕捉しない設計であり、既存の特性テスト（`_SimulatedRerunInterrupt(BaseException)`を用いる）が引き続き通ることも実行結果で確認した。
- 5節「是正実装を含まない」の遵守: 上記変更はいずれも診断目的のログ追加・切り分け機構であり、`_enqueue_job()`のダウンロード処理自体を非同期化する等の是正実装は含まれていないことを確認した。

## 4. 実機ログの照合と原因確定ロジックの検証

添付ログ`診断ログ実測_streamlit_repro3_異URL交絡排除版.log`を全文読み込み、報告書引用の抜粋（該当箇所）が原本と一致することを確認した。

- `token=310d0427`（1件目、`dQw4w9WgXcQ`）: `enqueue試行開始`→`_resolve_input開始`→（ダウンロード成功、`Audio extraction complete`実測）→`_resolve_input完了`まで到達した後、`spinnerブロック脱出`のログが一切出ないまま消失していることを、ログ全文中の`310d0427`出現箇所（全4件）を確認して裏付けた。
- `token=17834a3b`（2件目、`jNQXAC9IVRw`）: 最初から最後まで正常に完了し`item_id=1`としてキューに登録・処理開始されたことを確認した。
- `_resolve_input完了`ログと`spinnerブロック脱出`ログの間で実行されるコードが`with st.spinner(...):`ブロックの`__exit__`処理のみであることを、`webui.py`の該当箇所（L205-210付近）の実測で確認した。

**原因説明の技術的根拠を実測検証した**:

- Streamlit本体（`.venv`内、`streamlit/runtime/scriptrunner/script_runner.py` L456-505）の`_maybe_handle_execution_control_request()`のdocstring・実装（`RERUN`リクエスト時に`RerunException`を送出）を実測し、報告書の引用と一致することを確認した。
- `RerunException`が`ScriptControlException`経由で`BaseException`を継承すること（`except Exception`で捕捉不可）を`streamlit/runtime/scriptrunner_utils/exceptions.py`で実測確認した。
- `streamlit/elements/spinner.py`を実測し、`st.spinner()`の`finally`節が`enqueue_message(clear_transient())`を呼び出す（＝ForwardMsgを送信する＝ドキュメントに言う「暗黙のyieldポイント」に該当する）ことを確認した。

以上により、「`with st.spinner(...)`ブロックの`__exit__`が`RerunException`のyieldポイントとして機能する」という報告書の結論は、ログ証跡とStreamlit実装コードの両面から整合していると判断する。

## 5. クリーンアップの実機確認と軽微指摘

`output/history.db`の該当レコード（dQw4w9WgXcQ/jNQXAC9IVRw関連）は0件を確認した。

**軽微指摘（合格判定に影響なし）**: `output/queue_downloads/`配下に空ディレクトリが3件（`6335c58c`・`8df68ff0`・`9384ca3d`）残存していることを確認した。いずれも中身のファイルは削除済みで空ディレクトリのみが残る状態であり、実害はない。報告書は「7件のファイル削除完了」を報告しており、ファイル自体の削除という主張とは矛盾しないが、入れ物のディレクトリの削除（rmdir等）まで徹底することを次回以降の推奨事項として記録する。なお`0faf970a/`配下の`m6GaYaaJcpU`関連ファイルは、报告書記載のとおり采による既存の再現物であり、削除せず保持する扱いが妥当であることを確認した。

## 6. 合格判定前の3点自己反証

- **検証範囲**: 3節の原因確定ロジック全体（ログ照合・コード根拠双方）・機械的確認手順の実施状況・5節スコープ遵守、いずれも全数確認した。特に初回不合格の原因となった2論点（トークン引用・交絡要因）を重点的に再検証した。
- **証拠**: 判定根拠はsa自身が実行した`pytest`・`grep`、sa自身が全文照合したログファイル、sa自身が直接読解したStreamlit本体の実装コード（`script_runner.py`・`spinner.py`・`exceptions.py`）。sakuの自己申告のみに依拠していない。
- **判定**: 検出した不備は0件（軽微指摘1件は実害なしのため不合格理由としない）。中間評価・条件付き合格は出していない。

## 7. 計ロールへの報告

合格につき、featureブランチ（`feature/webui-job-queue-fix2-20260805`）へのコミット・dev反映、および采への原因確定結論の報告をお願いします。

**文書終了**

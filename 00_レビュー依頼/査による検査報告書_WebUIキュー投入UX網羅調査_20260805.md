# XXX番: 検査報告書 - WebUIキュー投入の体感面を含む網羅調査

> **発行日**: 2026年08月05日 / **発行者**: 査ロール（sa） / **文書種別**: 検査報告書
> **検査対象**: 作から計への作業完了報告_WebUIキュー投入UX網羅調査_20260805.md（feature/webui-queue-ux-investigation-20260805、作業ツリー）
> **宛先**: 計ロール

## 検査結果

### 判定: **合格**

## 1. セルフチェック証跡の確認

完了報告書のセルフチェックリスト6項目全て☑、署名・日付つき完了宣言を確認。品質検査に進んだ。

## 2. 機械検査の再実行と是正実装を含まないことの確認

| 検査項目 | 実行コマンド | 結果 | 判定 |
|---|---|---|---|
| 回帰テスト | `uv run python -m pytest -q` | `102 passed, 1 skipped in 6.77s` | ✅ 一致 |
| コード変更範囲 | `git diff --stat` | 出力なし（差分ゼロ） | ✅ 5節「是正実装を含まない」を実測で裏付け |

## 3. 本番`output/history.db`の整合性確認（重点確認事項、バックアップ全数照合）

現在の`output/history.db`の総件数（20件）を実測した上で、報告書添付のバックアップ（`backup/20260805_202224/history_backup_before_queueux_cleanup.db`、32件）と照合した。

- 削除された12件（id37-48）は、いずれも`output/uploads/qtest_ok.wav`・`qtest_a.wav`・`qtest_b.wav`・`qtest_c.wav`（ローカルファイル）または`jNQXAC9IVRw`（Me at the zoo）であり、saku投入テストの既知パターンと完全一致することを確認した。
- 残存するid32-36の5件（16:15〜20:03の広い時間帯に分散、うち`r4YWQU8WgD8`は本チケット発端となった采発見ケースの実データ）は投入テストと無関係の正当な利用履歴と判断でき、誤って削除されていないことを確認した。

**査読上の留意点**: 直前のtc-ops #441検査において、査（sa）自身がID連番の近接性のみに基づき正当な履歴を投入テスト由来と誤判定した経緯がある。本検査ではこの教訓を踏まえ、`source_original`の内容単位でバックアップとの差分を照合し、sakuのクリーンアップが正確であったことを確認した。

## 4. 実機グラウンディング検査

### 4-1. 最重要発見（パターン1b、表示順の逆転）

添付スクリーンショット②を確認し、完了済み一覧の先頭に`[失敗] https://www.youtube.com/watch?v=invalid00000`が表示されていることを確認した。`list(reversed(job_queue.finished))`（`finished`は`item_id`昇順の`self.items`から抽出、既存ラウンドのコード実測で確認済み）というコードロジックは、「後から投入して先に失敗確定した項目が先頭に表示される」という報告書の説明と整合する。

**軽微指摘（合格判定に影響なし）**: 本パターンのサーバーログ生ファイルが添付されておらず、検証インスタンス（ポート8502、手動起動）は`journalctl`にも記録が無いため、生ログでの再現照合はできなかった。ただし`reversed()`ロジック自体は決定論的なコードであり過去複数ラウンドの検査で実測済みのため、スクリーンショットとコード実測の組み合わせで十分な裏付けと判断した。新たな本番DB書き込みを伴う追加の実機再現は、production data保全の観点から見送った。

### 4-2. 識別不能問題（パターン1・5）

添付スクリーンショット①（同一URL2件）・⑥（同一URL3件）を確認し、いずれも`item.label`のみの表示で完全に同一の見た目になり、`item_id`等の識別情報が画面上に存在しないという報告書の指摘と一致することを確認した。

## 5. 合格判定前の3点自己反証

- **検証範囲**: `git diff`によるスコープ遵守確認・本番DB整合性（バックアップ全数照合）・パターン1b/1/5の実機照合、全数確認した。
- **証拠**: 判定根拠はsa自身が実行した`git diff`・`sqlite3`実測（バックアップとの差分照合、一次データ）・スクリーンショット確認・過去ラウンドで確認済みのコード実測。sakuの自己申告のみに依拠していない。
- **判定**: 検出した不備は0件（サーバーログ未添付は軽微な手続き指摘のみで、合格判定に影響しない）。中間評価・条件付き合格は出していない。

## 6. 計ロールへの報告

合格につき、featureブランチ（`feature/webui-queue-ux-investigation-20260805`）へのコミット・dev反映、および采への調査結果報告をお願いします。

**文書終了**

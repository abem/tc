# XXX番: 検査報告書 - WebUIジョブキュー処理化の是正（tc-ops #440是正）

> **発行日**: 2026年08月05日 / **発行者**: 査ロール（sa） / **文書種別**: 検査報告書
> **検査対象**: 作から計への作業完了報告_WebUIジョブキュー処理化是正_20260805.md（feature/webui-job-queue-fix-20260805、作業ツリー）
> **宛先**: 計ロール

## 検査結果

### 判定: **合格**

## 1. セルフチェック証跡の確認

完了報告書のセルフチェックリスト7項目全て☑、署名・日付つき完了宣言を確認。品質検査に進んだ。

## 2. 機械検査の再実行（saku自己申告を信じず、sa自身で再実行）

| 検査項目 | 実行コマンド | 結果 | 判定 |
|---|---|---|---|
| 回帰テスト | `uv run python -m pytest -q` | `93 passed, 1 skipped in 7.02s` | ✅ 既存91+新規2、回帰なし。saku・計報告値と一致 |
| 不具合2再現テスト存在確認 | `uv run python -m pytest -q --collect-only \| grep -i "duplicate\|重複"` | `test_duplicate_url_with_same_download_dir_collides_illustrating_the_bug`／`test_duplicate_url_with_distinct_download_dirs_does_not_collide` の2件 | ✅ 3節の完了条件充足 |
| 新規テスト単独実行 | `uv run python -m pytest -v tests/test_webui_duplicate_url_download.py` | `2 passed in 1.09s` | ✅ 全件PASS |

## 3. コード実測による是正内容の確認

`git diff --stat` を実行し、変更が `webui.py` のみ（15行追加／4行削除）であることを確認した。`core/cli_workflow.py`・`handlers/youtube.py` は差分0行、無変更であることを実測確認した（4-1節の採用方式どおり、既存関数のシグネチャ変更なし）。

`webui.py` の差分を実測し、以下を確認した:

- **不具合2是正**: `_enqueue_job()` が投入（enqueue）ごとに `download_dir = Path("output") / "queue_downloads" / uuid.uuid4().hex[:8]` を生成し、`_resolve_input(form_values, download_dir)` へ渡す。これが `resolve_input_audio()` の既存 `output_dir` 引数（無変更）へそのまま渡る配線になっていることを確認した。同一URLでも投入ごとに異なる `download_dir` となるため、`handlers/youtube.py` の決定的ファイル名生成（L111-115、無変更）が別ディレクトリ配下で行われ、ファイルパス衝突が構造的に回避される。
- **不具合1是正**: `_enqueue_job()` 内の `_resolve_input()` 呼び出しが `with st.spinner("入力を解決しています(ダウンロード等)...")` で包まれていることを確認した。
- **スコープ遵守**: ローカルアップロード分岐（`upload_dir = Path("output/uploads")` 固定）は無変更のまま。予備調査1-4節で報告された追加リスク（ローカルアップロードの同名ファイル上書き）は本チケットに含まれていないことを確認した。

## 4. 実機グラウンディング検査

添付スクリーンショット2枚（`00_レビュー依頼/添付_WebUIジョブキュー処理化是正_20260805/`）を確認し、コードとの整合を照合した。

| 項目 | スクリーンショット記載 | コード対応 | 一致 |
|---|---|---|---|
| 不具合1 | ①「キューに追加」クリック直後に「入力を解決しています(ダウンロード等)...」スピナー表示 | `st.spinner("入力を解決しています(ダウンロード等)...")` の文言と逐語一致 | ✅ |
| 不具合2 | ②同一URL（`dQw4w9WgXcQ`）の2件が異なる保存先（`output/20260805_090114_transcription.txt` / `_090049_`）・異なるGoogle Drive URLで完了表示、失敗0件 | `download_dir` の一意生成ロジック（3節）と整合 | ✅ |

報告記載のサーバーログ（1件目 `queue_downloads/26085e62/` ・2件目 `queue_downloads/255c1da9/` への分離、1件目完了時のcleanup後も2件目のファイルが存在）は、上記コード構造（投入ごとに異なる`download_dir`、各`QueueItem`が専有する`resolution`のみを`_cleanup_temp_file()`へ渡す既存設計）と矛盾せず整合する。

## 5. クリーンアップの実機確認

sa自身で以下を実行し、報告書記載の後片付けが実施済みであることを確認した。

```
$ find output/queue_downloads -type f   → ディレクトリ自体が存在せず(0件)
$ find output -iname "*20260805_0900*" -o -iname "*20260805_0901*"  → 該当なし(0件)
$ sqlite3経由 history.db の source_original LIKE '%dQw4w9WgXcQ%' 該当レコード → 0件
```

## 6. 軽微指摘（合格判定に影響なし、改善推奨）

完了報告書記載のバックアップパス `backup/20260805_090300/history_backup.db` は、実機では `backup/20260805_090323/history_backup.db`（秒表記の差異）。3節・4節の完了条件に該当しない参考情報のため、合格判定は維持する。前回チケット（WebUIジョブキュー処理化）検査時にも同種の軽微指摘があり、報告時の表記精度向上を継続して推奨する。

## 7. 範囲外事項の認識

完了報告書に開示されたGoogle Driveアップロード副作用（実在YouTube URLでの検証に伴う自動アップロード2件）は、計から采へ判断依頼済みの別件と計から伝達されている。本検査は技術是正内容（不具合1・2）の合否判定に限定し、当該副作用の要否判断は対象としない。

## 8. 合格判定前の3点自己反証

- **検証範囲**: 3節ゴール（機械検査2項目）・4-1節の是正方式の配線（是正前後の対照）・不具合1/2の実機照合・クリーンアップ、全数確認した。一部のみのサンプル確認ではない。
- **証拠**: 判定根拠はsa自身が実行した `pytest`・`grep`・`git diff`・`find`・`sqlite3` の出力、およびsa自身が読解した `webui.py` の実体差分（一次データ）。sakuの自己申告のみに依拠していない。
- **判定**: 検出した不備は0件（軽微指摘1件は要件非該当のため不合格理由としない）。中間評価・条件付き合格は出していない。

## 9. 計ロールへの報告

合格につき、featureブランチ（`feature/webui-job-queue-fix-20260805`）へのコミット・dev反映をお願いします。

**文書終了**

# (番号未採番・暫定発行): 検査報告書 - contextヒントの専用ファイル分離

> **発行日**: 2026年08月02日 / **発行者**: 査ロール(sa) / **文書種別**: 検査報告書
> **検査対象**: `config/config.yaml`・`core/utils.py`・`tc`・`docs/user-guides/configuration.md`・`tests/test_core_utils.py`・`config/context_hints.txt.sample`（新規）（ブランチ: `feature/asr-context-hint-file-2026-08-02`） / **宛先**: 計ロール(kei)
> **注記**: 前回同様、文書番号採番インフラ未整備のため番号なしの暫定発行とする(計承認済み)。

## 検査結果

### 判定: **合格**

## 1. 検査項目

| 確認項目 | 結果 | 備考 |
|---------|------|------|
| 対象ファイル一覧の一致 | ✅ | 5ファイル変更(config.yaml/utils.py/tc/configuration.md/test_core_utils.py)+新規1ファイル(context_hints.txt.sample)、報告のファイル一覧と完全一致 |
| 差分の数量(insertions/deletions) | ❌(報告値と不一致) | 査自身の`git diff --stat`実測=145 insertions/9 deletions(6ファイル計)。報告値「118 insertions/18 deletions」と一致しない(§5参照。合否判定への影響はコード内容自体が正しいため無し) |
| 構文正当性 | ✅ | `tc`/`core/utils.py`/`tests/test_core_utils.py`を`ast.parse`、`config/config.yaml`を`yaml.safe_load`で検査しOK |
| 単体テスト | ✅ | `uv run pytest -q` = 48 passed / 2 skipped / 0 failed（報告値と完全一致、新規4件含む） |
| core/config.py・core/transcription_interface.py無変更の確認 | ✅ | `git status`上いずれも変更なし。現在のHEAD(dev tip fa7c3d1)に前回マージ済みのcontext基盤がそのまま残っていることを確認 |
| .gitignore対象外の確認 | ✅ | `git check-ignore -v config/context_hints.txt` で `.gitignore:121:*.txt` にマッチすることを確認。`context_hints.txt.sample`は`.sample`拡張子のため対象外(untrackedとして正しく追加対象) |
| load_context_hints関数の単体動作 | ✅ | 査自身が直接呼び出して確認(§2.1) |
| 実機再現(tc全体・ローカル音声) | ✅ | 査自身が`./tc samples/e2e_sample.wav --no-upload`を実行して再現(§2.2) |
| 旧context直書きキーの残存参照 | ✅ | `whisper_config.get("context"` の残存参照をリポジトリ全体で検索し0件を確認(正しく`context_file`+`load_context_hints`へ置換されている) |

## 2. 証拠: 実機再現ログ(査自身による独立実行)

### 2.1 `load_context_hints`単体動作

```
nonexistent: ''
with hints: 'テスト用語XYZ123, 田中太郎'
after cleanup, exists: False
```

ファイル未存在時は空文字(後方互換)、ヒントファイル存在時はカンマ+半角スペース結合、検証後のクリーンアップも正常に行われることを確認した。

### 2.2 `tc`全体の実機再現(ローカル音声・`config/context_hints.txt`を一時的に作成)

```
$ printf '# test\nテスト用語XYZ123\n' > config/context_hints.txt
$ ./tc samples/e2e_sample.wav --no-upload
...
文字起こし完了: 12文字
文字起こし結果（最初の500文字）:
テスト用語XYZ123。
$ rm -f config/context_hints.txt
```

作の報告(ファイル未存在時=前回同様の後方互換動作、ヒント記載時=出力へ反映、検証後削除)と一致する結果を、査自身が独立に(`tc`スクリプト経由のエンドツーエンド実行で)再現した。

## 3. 単体テスト

```
$ uv run pytest -q
...............................ss.................                       [100%]
48 passed, 2 skipped in 1.23s
```

## 4. 合格判定前の3点自己反証

- **検証範囲**: 対象6ファイル(5変更+1新規)すべての差分内容を読解し、単体関数・エンドツーエンド実行の両レベルで機能検証した。core/config.py・transcription_interface.pyが「無変更」であることも`git status`で確認済み(申告のみを信じていない)
- **証拠**: 判定根拠はすべて査自身が直接実行したコマンド出力・直接読解したコード。§1の不一致(insertions/deletions数)も査自身の`git diff --stat`実測に基づく
- **判定**: 検出した不備(§5、数量不一致1件)は報告書の記載精度の問題であり、コード自体の正当性(機能・テスト・後方互換性)には影響しないと判断した根拠を明記した。条件付き合格は用いていない

## 5. 改善提案(合格判定とは別レイヤー)

### 5.1 完了報告の差分数量が実測と不一致

報告書記載: 「118 insertions/18 deletions」。査自身の実測(`git diff --stat`、対象6ファイル): **145 insertions(+) / 9 deletions(-)**。

内訳(査実測):
| ファイル | insertions | deletions |
|---|---|---|
| config/config.yaml | 4 | 1 |
| core/utils.py | 21 | 0 |
| docs/user-guides/configuration.md | 26 | 7 |
| tc | 2 | 1 |
| tests/test_core_utils.py | 58 | 0 |
| config/context_hints.txt.sample(新規) | 34 | 0 |
| **合計** | **145** | **9** |

ファイル一覧・各ファイルの実際の変更内容(コード・テスト・ドキュメント)はいずれも報告どおりで正当と確認できたため、本件は数量集計の誤りと推定される。合否判定には影響しないが、完了報告の数値精度として今後の参考のため記録する。

### 5.2 参考: scripts/gpu_monitor.pyの無関係な無コミット差分について

前々回指摘した`scripts/gpu_monitor.py`の無関係な差分(7 insertions/9 deletions)が本タスクのファイル一覧には含まれておらず、報告からも意図的に除外されている(申し送りどおり)。本タスクの検査対象外のため合否に影響しないが、作業ツリーには引き続き存在している。

## 6. 計ロールへの報告

判定は合格。§5.1(報告数値の不一致)は記録のみで対応不要。最終承認をお願いする。

**文書終了**

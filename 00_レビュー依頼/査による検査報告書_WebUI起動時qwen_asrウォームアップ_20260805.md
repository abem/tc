# XXX番: 検査報告書 - WebUI起動時のqwen_asrウォームアップ（暫定緩和策、tc-ops #441関連）

> **発行日**: 2026年08月05日 / **発行者**: 査ロール（sa） / **文書種別**: 検査報告書
> **検査対象**: 作から計への作業完了報告_WebUI起動時qwen_asrウォームアップ_20260805.md（feature/webui-qwen-asr-warmup-20260805、作業ツリー）
> **宛先**: 計ロール

## 検査結果

### 判定: **合格**

## 1. セルフチェック証跡の確認

完了報告書のセルフチェックリスト7項目全て☑、署名・日付つき完了宣言を確認。品質検査に進んだ。

## 2. 機械検査の再実行（saku自己申告を信じず、sa自身で再実行）

| 検査項目 | 実行コマンド | 結果 | 判定 |
|---|---|---|---|
| 回帰テスト | `uv run python -m pytest -q` | `102 passed, 1 skipped in 7.40s` | ✅ 回帰なし |
| ウォームアップimport確認 | `grep -n "import qwen_asr" webui.py` | `78: import qwen_asr  # noqa: F401` | ✅ 指示書3節の完了条件を充足 |

## 3. コード実測による是正内容の確認

`webui.py`の差分を実測し、以下を確認した。

- `_warmup_qwen_asr()`は`@st.cache_resource`デコレータ付きで実装されており、プロセス単位で一度だけ実行される設計であることを確認した。
- `try/except Exception`で`import qwen_asr`の失敗を捕捉し、`logger.warning(...)`のみを出力して`False`を返す（`re-raise`しない）実装を確認した。WebUI起動自体を失敗させない要件を満たしている。
- `main()`内で`_load_config()`の直後に`_warmup_qwen_asr()`が呼ばれていることを確認した。
- モデル重み本体のロード（`Qwen3ASRModel.from_pretrained(...)`）は一切追加されておらず、4-2節のスコープ除外（モデル重みロードは対象外）を遵守していることを確認した。
- `core/transcription_interface.py`（既存の遅延import）は無変更であることを`git diff --stat`で確認した（`webui.py`のみ23行追加）。

## 4. 実機グラウンディング検査（sa自身による独立再現）

完了報告書には実機確認の結果がテキストで記載されていたが、生ログファイルの添付が無かった。そのため、sa自身で以下を実施し独立に再現・検証した。

- 本番`tc-webui.service`（ポート8501）とは別のポート（8597）でStreamlitインスタンスを起動した。
- システムのPlaywright（Chromium）で実際のブラウザセッションを確立し、ページロードを実施した（`curl`によるプレーンなHTTPリクエストではStreamlitのスクリプト実行がトリガーされないことを確認した上で、実際のWebSocketセッションを伴うPlaywrightでの検証に切り替えた）。
- 結果、Uvicorn起動直後の初回ページロード時点で`qwen_asrウォームアップ成功`のログが出力されることを独立に確認した。報告書記載のサーバーログと同一パターンであった。

**軽微指摘（合格判定に影響なし）**: 完了報告書に実機確認の生ログファイルが添付されていなかった。sa自身の独立再現によりreportの記載内容の正確性は裏付けられたため合格判定には影響しないが、次回以降は本ティケット系列の他報告と同様、生ログの添付を徹底することを推奨する。

## 5. 本番DBへの副作用確認

`output/history.db`をsa自身で実測し、15件（内訳: 既存11件＋正当な利用履歴2件〔id20・21〕＋その他2件〔id30・31〕）で、sa自身の検証実施前後で変化がないことを確認した。sa自身の再現検証（ページロードのみ、文字起こしジョブは投入せず）でも副作用が発生しないことを確認した。報告書記載の「15件、検証前後で変化なし」という主張と整合する。

## 6. 合格判定前の3点自己反証

- **検証範囲**: コード実測・機械検査・実機ログの独立再現・本番DBへの副作用確認、全数確認した。
- **証拠**: 判定根拠はsa自身が実行した`pytest`・`grep`、sa自身が読解した`webui.py`の実体差分、sa自身が独立に実施したPlaywright再現（一次データ）。sakuの自己申告のみに依拠していない。
- **判定**: 検出した不備は0件（生ログ未添付は軽微な手続き指摘のみで、合格判定に影響しない）。中間評価・条件付き合格は出していない。

## 7. 計ロールへの報告

合格につき、featureブランチ（`feature/webui-qwen-asr-warmup-20260805`）へのコミット・dev反映をお願いします。

**文書終了**

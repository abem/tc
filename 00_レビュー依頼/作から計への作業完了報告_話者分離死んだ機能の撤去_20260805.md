# 作から計への作業完了報告: 話者分離機能（未実装・死んだコード）の撤去 完了

**報告日**: 2026-08-05
**報告者**: 作成ロール（作／saku）
**対象**: 計から作への作業指示書_話者分離死んだ機能の撤去_20260805.md（承認済み実装計画:
作から計への予備調査完了報告_話者分離死んだ機能の撤去_20260805.md、
作から計への実装計画確定報告_話者分離死んだ機能の撤去_20260805.md）

---

## 1. 完了報告

- 話者分離機能（未実装・死んだコード）の完全撤去: 完了 ✅
- 変更ファイル一覧:
  - `webui.py`: 「話者分離を有効化」チェックボックス・`settings["diarization"]`・
    `DiarizationConfig`生成・importを削除。`build_output_file()`呼び出しの引数も削除。
  - `transcribe.py`: `--diarization/-d`フラグ・CLIプロンプト・プリセット"2"/"4"
    （話者分離付きプリセット、番号は欠番のまま他プリセットへの影響を避けた）・
    `DiarizationConfig`関連・`Confirm`未使用import・`build_output_file()`引数を削除。
  - `core/transcription_interface.py`: `_initialize_diarization()`・`_transcribe_with_speakers()`・
    `_extract_audio_segment()`・`UnifiedTranscriber.__init__`の`diarization_config`引数・
    `get_stats()`の関連項目・`create_transcriber()`の引数を削除。未使用となった
    `_get_audio_duration()`（`_transcribe_with_speakers`専用だった）も削除。
    **タイムスタンプ機能（ForcedAligner、L478-782相当）は一切変更していません**。
  - `core/config.py`: `DiarizationConfig`クラス全体・`UnifiedConfig`内の関連フィールド・
    `create_for_use_case()`内の`diarization`参照（"multi_speaker_meeting"ケース自体は
    transcription設定のみ残し存置）・`to_dict()`/`from_dict()`の関連処理を削除。
  - `core/__init__.py`: `DiarizationConfig`のimport・`__all__`エントリを削除。
  - `core/model_manager.py`: `DiarizationModelLoader`クラス・登録箇所を削除。
  - `core/cli_common.py`: `build_output_file()`の`diarization_enabled`引数を削除
    （呼び出し元`webui.py`・`transcribe.py`とも追従済み）。
  - `core/cli_workflow.py`: **DBスキーマ（`diarization_enabled`列）は無変更**、
    `settings.get("diarization", False)`へ変更のみ（既存本番データ保護のため保守的対応）。
  - `config/config.yaml`: `speaker_diarization`セクションを削除。
  - `speaker_diarization.py`: ファイル削除（未使用単体スクリプト、依存未インストール、
    `hf_token`未定義バグにより実質動作不能だった）。
  - `tests/test_core_config.py`: `TestDiarizationConfig`クラスを削除。
  - `tests/test_core_webui_workflow_queue.py`・`tests/test_webui_enqueue_interruption.py`・
    `tests/test_webui_queue_display_order.py`: `_settings()`ヘルパー内のもう存在しない
    `"diarization"`キーを削除（テストデータの整合性維持、追加発見・独自対応）。
- 所要時間: 予備調査〜実装完了まで約50分

## 2. 品質検査結果

### 機械検査
```
$ uv run python -m pytest -q
106 passed, 1 skipped in 7.58s
```
既存108 passed/1 skippedから、`TestDiarizationConfig`削除（2件）を反映し106 passed（回帰なし、
削除以外のテストは全て合格）。

```
$ grep -n "話者分離" webui.py
(該当なし)

$ grep -rn "core.diarization_engine\|DiarizationEngine" --include="*.py" .
(該当0件)

$ uv run python -m pytest -q tests/test_core_transcription_interface_timestamps.py
11 passed in 5.48s
```
すべて指示書3節の完了条件を充足。タイムスタンプ機能（ForcedAligner）は無傷であることを確認済み。

### 残存参照の網羅確認（証拠ベース規律）

```
$ grep -rln "diarization\|Diarization" --include="*.py" --include="*.yaml" . | grep -v backup | grep -v __pycache__
setup_huggingface.py
core/cli_workflow.py
tests/test_webui_history_cleanup.py
```
上記3件は意図的に残した箇所のみです:
- `core/cli_workflow.py`: DBスキーマ・`settings.get(...)`（2-1節どおり無変更方針）
- `tests/test_webui_history_cleanup.py`: 履歴DBのDDL（DBスキーマ無変更のため、テスト用DDLも
  合わせて無変更）
- `setup_huggingface.py`: 計判断どおりスコープ外として据え置き

**注記（開示）**: `setup_huggingface.py` L139の利用例`./transcribe.py audio_file.wav
--diarization`は、`--diarization`フラグ削除に伴い実際には動作しなくなります（未指摘のまま
残る既知の陳腐化）。計から采への次チケット候補報告に含めていただくようお願いします
（判断結果メッセージで確認済みの対応方針どおり）。

## 3. 判断結果との対応関係

| 質問（予備調査5節） | 判断結果 | 実装での対応 |
|---|---|---|
| 1. `DiarizationConfig`等 | 完全削除 | 実装どおり |
| 2. `config.yaml` `speaker_diarization` | 完全削除 | 実装どおり |
| 3. `transcribe.py`の`--diarization`等 | 完全削除 | 実装どおり |
| 4. `speaker_diarization.py` | 完全削除 | 実装どおり |
| 5. `DiarizationModelLoader` | 完全削除 | 実装どおり |
| 2-1（DBスキーマ） | 列無変更、`settings.get`のみ変更 | 実装どおり |
| 2-2（`build_output_file`引数） | 引数削除 | 実装どおり |

## セルフチェックリスト
- [x] 成果物はすべて作成/更新した
- [x] 命名規則は遵守されている
- [x] フォーマットや構造は要件通り
- [x] 誤字脱字、記述ミスはない
- [x] リンク切れや参照エラーはない
- [x] スコープ外の作業を含んでいない（タイムスタンプ機能は無変更、`setup_huggingface.py`等は
      計判断どおり据え置き）
- [x] スクリプトは正常に動作する（機械検査で確認）
- [x] `git diff --summary`でexec bit変更が無いことを確認済み

## WBS更新依頼

WBSの更新をお願いします。

---

**セルフチェック完了宣言:**
私、作ロールは、上記チェックリストに基づきセルフチェックを実施し、すべての項目を満たしていることを宣言します。

**署名**: 作ロール
**日付**: 2026年08月05日

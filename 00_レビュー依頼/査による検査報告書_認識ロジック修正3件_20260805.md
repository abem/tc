# 検査報告書：認識ロジック修正3件

> **発行日**: 2026年08月05日 / **発行者**: 査ロール（sa） / **文書種別**: 検査報告書
> **検査対象**: saku完了報告分（`feature/asr-context-fix-and-cleanup-20260805` ブランチ、作業ツリー）
> 変更ファイル: `core/transcription_interface.py`（項目1）、`transcribe.py`（項目2）、
> `config/config.yaml`（項目3）、`README.md`・`docs/user-guides/configuration.md`（計判断の追加同期）
> **宛先**: 計ロール（kei）
> **関連**: 計から作への作業指示書_認識ロジック修正3件_20260805.md（4-1〜4-3節）

---

## 検査結果

### 判定: **合格**

---

## 1. 機械検査の再実行

| # | 検査内容 | コマンド | 結果 |
|---|---------|---------|------|
| 2 | `transcribe.py`へのcontext配線 | `grep -n "context=" transcribe.py` | OK（L172に`context=load_context_hints(...)`実在） |
| 3 | `config.yaml`の`language_models`削除 | `grep -c "language_models:" config/config.yaml` | OK（0） |
| 共通 | 回帰テスト | `uv run python -m pytest -q` | OK（**83 passed, 1 skipped**、saku報告と一致） |

---

## 2. 4-1（項目1: context注入の幻覚リスク是正）の実機照合・独立動的検証

### コード実測

`git diff`実測により、`chunk_context = context if i == 0 else ""`を導入し、初回・リトライ呼び出し
双方で`context=chunk_context`（旧`context=context`）に変更されていることを確認した。単一チャンク経路
（`context=self.config.context`、L610）は無変更であり、スコープ厳守を確認した。

### 独立動的検証（sa自身によるモック実行）

saku報告の「chunk0のみcontext非空、chunk1以降空文字」に加え、`Qwen3ASREngine`をモック化した
動的テストをsa自身で実施した。

**シナリオ1（通常実行、3チャンク音声）**:

```
chunk 0: context='固有名詞ヒント'
chunk 1: context=''
chunk 2: context=''
```

**シナリオ2（反復ループ検出によるリトライを強制発生、chunk0・chunk1双方）**:

```
call 1 (chunk0 初回): context='固有名詞ヒント'
call 2 (chunk0 リトライ): context='固有名詞ヒント'  ← 初回と同一値を正しく再利用
call 3 (chunk1 初回): context=''
call 4 (chunk1 リトライ): context=''  ← 空文字が漏れなく維持
call 5 (chunk2 初回): context=''
```

反復検出・再試行の既存ロジック自体も正常動作（2/3チャンクで`retry succeeded`）することを確認した。
saku報告にはリトライ経路でのcontext値検証が明記されていなかったため、この点はsaが独自に追加検証した。

---

## 3. 4-2（項目2: エントリーポイント間の機能不整合解消）の実機照合

`transcribe.py`に`from core.utils import load_context_hints`が追加され、
`UnifiedConfig.get("whisper", "context_file", default="config/context_hints.txt")`の引数が
`core/config.py` L209 `def get(cls, *keys, default=None)`のシグネチャと整合することを確認した。

---

## 4. 4-3（項目3: config.yamlのdead codeクリーンアップ）の実機照合

`config.yaml`の`whisper.language_models`セクション（コメント含む）が削除されていることを確認した。
`core/cli_common.py`の`select_model()`は無変更（削除禁止要件を遵守）、同ファイル自体に差分がないことを
確認した。

### スコープ外追加（README.md・docs/user-guides/configuration.md）

要件4-3-3「他の記述の確認・列挙、削除は計判断」条項に基づく計判断としてsaku報告と整合する。
リポジトリ全体`grep`実測では他に`docs/system-docs/system_overview_2025.md`等にも同種の記載が
残存しているが、2026-08-02付の既存検査報告書（査による検査報告書_クローズ前improvement2点_20260802.md）
で「本タスクの合否には無関係、将来是正候補」と既に記録済みの既知事項であり、本要件（config.yaml以外は
基本的に対象外）にも合致するため不合格理由としない。

---

## 5. 合格判定前の3点自己反証

- **検証範囲**: 作業指示書4-1〜4-3節の要件全数、機械検査2項目、pytest、項目1のリトライ経路
  （saku未検証部分を追加補完）。限定なし。
- **証拠**: 判定根拠はsa自身が実行した`git diff`実測・モックによる動的実行結果（saku自己申告を
  証拠代わりにしていない）。
- **判定**: 不備0件。中間評価（条件付き合格）は行っていない。

---

## 6. 計ロールへの報告

合格につき、featureブランチへのコミット→dev反映をお願いします。

**文書終了**

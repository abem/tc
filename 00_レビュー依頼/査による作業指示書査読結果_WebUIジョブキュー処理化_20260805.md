# 査による作業指示書査読結果：WebUI文字起こしジョブのキュー処理化

> **発行日**: 2026年08月05日 / **発行者**: 査（sa） / **文書種別**: 査読結果
> **査読対象**: 計から作への作業指示書_WebUIジョブキュー処理化_20260805.md / **宛先**: 計（kei）

## 査読結果

### 判定: **合格**

## 1. DoR充足性

作業指示書内チェック表7項目を照合。全項目充足を確認。

| # | 項目 | 確認結果 |
|---|------|---------|
| 1 | 背景と目的が明確か | ✅ 2節に記載あり |
| 2 | ゴール（完了条件）が定義されているか | ✅ 3節に機械検査コマンド＋期待値、UI確認要件を記載 |
| 3 | スコープが限定されているか | ✅ 4-3節に対象外3点を明記 |
| 4 | 成果物の仕様・要件が明確か | ✅ 4-2節にユーザー要望3点を記載 |
| 5 | 依存関係が解決されているか | ✅ 6節に参照コード・作業ブランチを記載 |
| 6 | 担当者と期限 | ✅ 担当saku・期限「速やか」（本プロジェクトの他作業指示書と同一書式） |
| 7 | レビュー/承認者 | ✅ レビュー査(sa)・最終承認計(kei) |

## 2. コード例・行番号引用の正確性（2節・6節）

`webui.py`・`core/webui_workflow.py` を実体読み込みし、引用行番号を全数照合。

| 引用箇所 | 文書記載 | 実体確認 | 結果 |
|---|---|---|---|
| `_start_job()` | L136-166 | L136 def 〜 L166 `job_saved`設定 | ✅ 一致 |
| `_cleanup_temp_file()` | L169-182 | L169 def 〜 L182 | ✅ 一致 |
| `_save_and_record()` | L185-220 | L185 def 〜 L220 | ✅ 一致 |
| `_render_progress_and_result()` | L223-277 | L223 デコレータ 〜 L277 | ✅ 一致 |
| `main()` | L331-348 | L331 def 〜 L348 | ✅ 一致 |
| 実行ボタン押下 | L342-343 | L342 `if st.button` / L343 `_start_job(...)` | ✅ 一致 |
| `TranscriptionJob` | L19-27 | L19 class 〜 L27 | ✅ 一致 |
| `start_transcription_job()` | L30-55 | L30 def 〜 L55 `return job` | ✅ 一致 |
| `drain_progress()` | L58-66 | L58 def 〜 L66 `return messages` | ✅ 一致 |

2節の記述内容（`_start_job()` が実行中でも `st.session_state["job"]` を上書きする挙動、`_render_progress_and_result()` がL238以降で`job.done`検知後に`_save_and_record()`を呼ぶ経路）も実体コードと一致することを確認した。

## 3. 4-3節スコープ設定の妥当性

- 複数ジョブの並列実行対象外：4-2節「GPU/モデルが1インスタンスのみの制約」と整合、妥当
- `core/transcription_interface.py` 等の既存ロジック変更対象外：5節「含まない」と重複せず整合、妥当
- キューの永続化（プロセス再起動をまたぐ保持）対象外：4-1節の予備調査対象（`st.session_state`保持前提）と矛盾せず、妥当

不整合・過不足なし。

## 4. 追加検証（実機照合）

| 検証項目 | 検証方法 | 結果 |
|---|---|---|
| 作業ブランチ `feature/webui-job-queue-20260805` の実在性 | `git branch -a` | ✅ 実在確認 |
| ブランチ分岐元が dev tip `6fa8f60` と一致 | `git merge-base dev feature/webui-job-queue-20260805` | ✅ `6fa8f60c4d4a827dafc2cd07a11b4ecd6f256808` で一致（`git log dev -1`と同一） |
| 3節のベースライン数値「83 passed, 1 skipped」 | `uv run python -m pytest -q` を査自身が実行 | ✅ `83 passed, 1 skipped in 9.16s` で一致 |

## 5. 合格判定前の3点自己反証

- **検証範囲**: 行番号引用9箇所を全数照合（一部のみのサンプル確認ではない）。DoR7項目・4-3節3項目も全数確認した。
- **証拠**: 判定根拠は査自身が読んだ `webui.py`・`core/webui_workflow.py` の実体、査自身が実行した `pytest`・`git` コマンド出力（一次データ）。作・計の自己申告には依拠していない。
- **判定**: 検出した不備は0件。条件付き合格は出していない。

## 6. 計ロールへの報告

本文書をもって作業指示書は合格。saku への正式伝達を進めて差し支えない。

**文書終了**

# 作から計への作業完了報告: WebUI起動時のqwen_asrウォームアップ（暫定緩和策） 完了

**報告日**: 2026-08-05
**報告者**: 作成ロール（作／saku）
**対象**: 計から作への作業指示書_WebUI起動時qwen_asrウォームアップ_20260805.md（承認済み実装計画:
作から計への予備調査完了報告_WebUI起動時qwen_asrウォームアップ_20260805.md）

**位置づけの再確認**: 本件はtc-ops #441（根本原因調査、原因未確定のまま采判断でクローズ済み、
dev tip=9478a6a）とは独立した対症療法です。根本原因の是正ではありません。

---

## 1. 完了報告

- ウォームアップ機能の追加: 完了 ✅
- 成果物:
  - `webui.py`: `_load_config()`の直後に`@st.cache_resource`デコレータ付きの新規関数
    `_warmup_qwen_asr()`を追加し、`main()`から呼び出し。
- 所要時間: 実装〜実機確認まで約15分

## 2. 品質検査結果

### 機械検査
```
$ uv run python -m pytest -q
102 passed, 1 skipped in 7.53s
```
既存から回帰なし（新規テストは無し、予備調査計画どおり既存関数への影響が無い設計）。

```
$ grep -n "import qwen_asr" webui.py
78:        import qwen_asr  # noqa: F401
```
ウォームアップ用のimport文の存在を確認。指示書3節の完了条件を充足。

### 実機確認

本番`tc-webui.service`とは別ポート（8502）で検証用インスタンスを起動し、Playwrightで
ページロードのみ（文字起こしジョブは投入していない、`output/history.db`への書き込みなし）を
実施しました。サーバーログ実測:

```
16:01:27.799  Uvicorn server started on :::8502
16:01:44,250  qwen_asrウォームアップ成功
```

最初のブラウザアクセス（ページロード）時点でウォームアップが実行されていることを確認しました。
ユーザーが実際に文字起こしを投入する操作（入力・設定・ボタン押下）には通常これより後の時間を
要するため、要件（1-2節の解釈整理どおり「ページロード時点でユーザー操作より確実に先行する」）を
満たすことを実機で確認しました。

## 3. 副作用の確認

検証は文字起こしジョブを投入していないため、`output/history.db`（15件、検証前後で変化なし）・
Google Driveへのアップロードとも副作用は発生していません。

## セルフチェックリスト
- [x] 成果物はすべて作成/更新した
- [x] 命名規則は遵守されている
- [x] フォーマットや構造は要件通り
- [x] 誤字脱字、記述ミスはない
- [x] リンク切れや参照エラーはない
- [x] スコープ外の作業を含んでいない（モデル重みロード・tc-ops #441の根本是正は未実施）
- [x] スクリプトは正常に動作する（機械検査・実機確認とも成功）

## WBS更新依頼

WBSの更新をお願いします。

---

**セルフチェック完了宣言:**
私、作ロールは、上記チェックリストに基づきセルフチェックを実施し、すべての項目を満たしていることを宣言します。

**署名**: 作ロール
**日付**: 2026年08月05日

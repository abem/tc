# 計から作への作業指示書：README.mdへのWebUI説明追記

**発信**: 計（kei）
**宛先**: 作（saku）
**日付**: 2026-08-05
**種別**: 作業指示書（文書のみ・査読省略、采承認済み）
**関連**: 采から計への方針指示（全員待機の例外としてこの1件のみ着手可、2026-08-05、ユーザー指摘）

---

## 1. DoR (Definition of Ready) チェック

| # | 項目 | 判定 |
|---|------|------|
| 1 | 背景と目的が明確か | ☑ |
| 2 | ゴール（完了条件）が定義されているか | ☑ |
| 3 | スコープ（範囲）が限定されているか | ☑（文書のみ、コード変更なし） |
| 4 | 成果物の仕様・要件が明確か | ☑ |
| 5 | 依存関係が解決されているか | ☑ |
| 6 | 担当者と期限が設定されているか | ☑（担当: saku、期限: 速やか） |
| 7 | レビュー/承認者が明確か | ☑（レビュー: 計(kei)。文書のみのため査(sa)検査は采の判断で省略可） |

## 2. 背景と目的

ユーザー指摘（2026-08-05）に基づく。`README.md`にWebUI（`webui.py`）の説明が一切無いことを
采が確認した（`grep -i "webui\|streamlit" README.md` 該当0件、計も本文書作成時に再確認済み）。
本案件は「全員待機」（sai方針指示、2026-08-05T03:35）の下にあるが、本件のみ例外として着手が
許可されている。**完了後は再び待機に戻ること。**

## 3. ゴール（完了条件）

```bash
grep -i "webui\|streamlit" README.md
# 期待: 該当行が複数存在すること
```

かつ、4節に列挙する3項目を実機で再確認した上で記載していること（計が一次確認する）。

## 4. 成果物の仕様・要件

`README.md`の「💻 使用方法」節（L106-139）の後ろ、「🔧 設定」節（L140-）の前に、新規サブ節
「WebUI（Streamlitプロトタイプ）」を追記する。**配置位置の微調整はsaku判断でよい。**

采提供の事実（日数経過・環境差異により陳腐化しうるため、執筆前に実機で再確認し、確認できた
実測値で記載すること。確認せず転記しない）:

1. **起動方法**: `uv run streamlit run webui.py --server.headless true --server.port 8501`
   （または systemd常駐化、詳細は`docs/system-docs/webui_architecture.md`参照とリンクする）
2. **機能概要**:
   - 文字起こしタブ: URL（YouTube/Google Drive）入力またはローカルファイルアップロード、
     モデル/デバイス/言語設定、話者分離、タイムスタンプ付与、認識ヒント（`context_hints`）
   - 複数入力をキューに投入し、現在の処理完了後に自動で次を開始する機能（tc-ops #440、
     同時並列は不可）
   - 履歴タブ: 変換履歴の日付絞り込み閲覧
3. **内部構成の詳細は本節に重複記載しない**: サーバースタック・systemd常駐化等の詳細は
   `docs/system-docs/webui_architecture.md`（既存、案件で作成済み）へのリンクに留める。

## 5. 作業範囲（スコープ）

**含む**:
- `README.md`への新規サブ節追記
- 執筆前の実機再確認（`uv run streamlit run webui.py`の起動確認、機能一覧の実機照合）

**含まない**（越権注意）:
- Git操作（コミット・プッシュは計が専任）
- コード変更（`webui.py`等、実装ファイルの変更は一切含まない）
- `docs/system-docs/webui_architecture.md`の内容変更（リンクのみ、重複記載はしない）
- 他の`README.md`セクションの変更（本サブ節追加のみ）

## 6. 依存関係と提供資料

- 采方針指示（全員待機の例外許可、2026-08-05T04:05:13）
- 参照文書: `docs/system-docs/webui_architecture.md`（既存、内部構成の詳細リンク先）
- 作業ブランチ: `feature/readme-webui-doc-20260805`（計が作成済み、dev tip c1b528c より分岐）

## 7. 承認プロセス（簡略フロー、采承認済み）

1. saku が実機再確認の上で追記し、計へ完了報告
2. 計が物理存在・3項目の記載・実機再確認の実施を一次確認
3. **文書のみの変更のため、査（sa）検査は省略する（采の判断・指示、従来の
   webui_architecture.md追記と同様の扱い）**
4. 計がfeatureブランチへコミット→dev反映を実施し、采へ報告
5. **完了後、再び全員待機の状態に戻ること**

---

本文書は文書のみの変更のため査（sa）への査読依頼は省略し、直接 saku へ正式伝達する（采指示）。

# 作から計への作業完了報告: WebUIジョブキュー処理化の是正 完了

**報告日**: 2026-08-05
**報告者**: 作成ロール（作／saku）
**対象**: 計から作への作業指示書_WebUIジョブキュー処理化是正_20260805.md（承認済み実装計画:
作から計への予備調査完了報告_WebUIジョブキュー処理化是正_20260805.md）

---

## 1. 完了報告

- 不具合1（UXフィードバック不足）・不具合2（同一URL重複投入失敗）: いずれも是正完了 ✅
- 成果物:
  - `webui.py`:
    - `_resolve_input(form_values, download_dir: Path)`: `download_dir`引数を追加。YouTube/X経路は
      `resolve_input_audio()`の既存`output_dir`引数へ渡す（ローカルアップロード分岐は指示どおり無変更）
    - `_enqueue_job()`: 投入(enqueue)ごとに`Path("output")/"queue_downloads"/uuid.uuid4().hex[:8]`
      を生成して`_resolve_input()`へ渡す（不具合2是正）。`_resolve_input()`呼び出しを
      `st.spinner("入力を解決しています(ダウンロード等)...")`で包む（不具合1是正）
  - `core/cli_workflow.py` / `handlers/youtube.py`: **無変更**（予備調査1-1節の設計どおり）
  - `tests/test_webui_duplicate_url_download.py`: 新規（2件、不具合2の再現テスト）
- 所要時間: 予備調査〜検証まで約40分

## 2. 品質検査結果

### セルフチェックリスト
- [x] 成果物はすべて作成/更新した
- [x] 命名規則は遵守されている
- [x] フォーマットや構造は要件通り（4-1節の設計決定どおりに実装）
- [x] 誤字脱字、記述ミスはない
- [x] リンク切れや参照エラーはない
- [x] スコープ外の作業を含んでいない（ローカルアップロードの同名ファイル上書き是正は含めていない。
      次チケットとして計が別途起票する方針を承知している）
- [x] スクリプトは正常に動作する（下記機械検査・実機確認のとおり）

### 機械検査
```
$ uv run python -m pytest -q
93 passed, 1 skipped in 7.54s
```
既存91 passed, 1 skippedから回帰なし。新規テスト2件すべて合格。

```
$ uv run python -m pytest -q --collect-only 2>&1 | grep -i "duplicate\|重複"
tests/test_webui_duplicate_url_download.py::TestDuplicateUrlDownloadPathCollision::test_duplicate_url_with_same_download_dir_collides_illustrating_the_bug
tests/test_webui_duplicate_url_download.py::TestDuplicateUrlDownloadPathCollision::test_duplicate_url_with_distinct_download_dirs_does_not_collide
```
指示書3節の完了条件（1件以上）を充足。上記1件目は是正前コード相当（同一`output_dir`）を再現し
衝突することを確認する対照ケース、2件目が実際の是正内容（異なる`output_dir`）で衝突しないことを
確認するケース。

### 実機操作ログ（Streamlit実サーバー + Playwright、実際のYouTube URLで検証）

**不具合1（UXフィードバック不足）の確認**: 「キューに追加」クリック直後、
`page.wait_for_selector("text=入力を解決しています")`で明示的に検出（タイムアウトなしで即座に検出）。
スクリーンショット添付①（`入力を解決しています(ダウンロード等)...`スピナーがボタン直下に表示）。

**不具合2（同一URL重複投入）の確認**: 同一URL（`https://www.youtube.com/watch?v=dQw4w9WgXcQ`、
3:33の実動画）を2回連続投入。サーバーログ実測:
```
2026-08-05 09:00:01,120 - Audio extraction complete: output/queue_downloads/26085e62/Rick-Astley-...wav
2026-08-05 09:00:08,633 - Audio extraction complete: output/queue_downloads/255c1da9/Rick-Astley-...wav
2026-08-05 09:00:48,975 - Completed: transcribe_Rick-Astley-...wav in 47.686s (1795 characters)
2026-08-05 09:00:52,678 - Temporary file removed: output/queue_downloads/26085e62/Rick-Astley-...wav
2026-08-05 09:01:13,880 - Completed: transcribe_Rick-Astley-...wav in 21.201s (1795 characters)
2026-08-05 09:01:17,066 - Temporary file removed: output/queue_downloads/255c1da9/Rick-Astley-...wav
```
→ 1件目(`26085e62/`)・2件目(`255c1da9/`)が異なるディレクトリに解決され、1件目完了時の
cleanup(`Temporary file removed`)が2件目のファイルへ波及していないことを確認。両ジョブとも
`failed_count=0`・完了済み一覧2件（スクリーンショット添付②、いずれも文字起こし結果1795文字が
表示され、`[失敗]`は0件）。是正前コードでは2件目が「Audio file not found」で失敗していたのに対し、
是正後は2件とも成功することを実機で確認した。

**スクリーンショット添付**（`00_レビュー依頼/添付_WebUIジョブキュー処理化是正_20260805/` に配置）:
- ① `①不具合1_スピナー表示.png`
- ② `②不具合2_同一URL2件完了.png`

### 実機検証に伴う副作用の開示（判断依頼）

不具合2の検証には実在のYouTube動画URLを使用したため、既存仕様どおり文字起こし結果がGoogle Drive
へアップロードされました（`resolution.source_type in {"youtube","gdrive"}`時の既存自動アップロード
挙動、本チケットでは変更していません）。検証で計2件（本文書「1.完了報告」の検証手順に対応する
2回分）を実際にアップロードしています。`GDriveClient`に削除メソッドが存在しないため
（`grep -n "def delete" handlers/gdrive.py`該当0件）、作の権限では削除を実施していません。
該当ファイルURL:
- `https://drive.google.com/file/d/16IbwSWRaCRKJSrBZHNwkDLLbznCi0jVO/view?usp=drivesdk`
- `https://drive.google.com/file/d/1rDoUTJ6WZDuulRSEHG3UZJFQXSIkDtjn/view?usp=drivesdk`

削除要否のご判断をお願いします。ローカルの生成物（`output/queue_downloads/`残存分・生成された
文字起こしファイル・`output/history.db`の検証用レコード2件）は検証後に削除済みです
（削除前に`backup/20260805_090300/history_backup.db`へBU取得済み）。

## 3. 予備調査時の設計決定との対応関係

| 予備調査時の決定事項 | 実装での対応 |
|---|---|
| output_dirのジョブ単位一意化(第3案、既存関数無変更) | `_enqueue_job()`が`uuid.uuid4().hex[:8]`で一意な`download_dir`を生成、`resolve_input_audio()`の既存`output_dir`引数へそのまま渡すのみ |
| CLI経路への影響ゼロ | `core/cli_workflow.py`・`handlers/youtube.py`は無変更、`transcribe.py`の呼び出し箇所も無変更 |
| gdrive経路は対応不要 | 無変更（`tempfile.NamedTemporaryFile`により既に一意） |
| st.spinner()による不具合1対応 | `_enqueue_job()`内`_resolve_input()`呼び出しを`with st.spinner(...)`で包む |
| ローカルアップロード同名ファイル上書きは対象外 | `_resolve_input()`のローカルアップロード分岐は無変更 |

## WBS更新依頼

WBSの更新をお願いします。

---

**セルフチェック完了宣言:**
私、作ロールは、上記チェックリストに基づきセルフチェックを実施し、すべての項目を満たしていることを宣言します。

**署名**: 作ロール
**日付**: 2026年08月05日

# 作から計への予備調査完了報告：話者分離機能撤去に伴う陳腐化ドキュメントの整理

**発信**: 作（saku）
**宛先**: 計（kei）
**日付**: 2026-08-05
**種別**: 予備調査完了報告（4-1節、実施前の計承認待ち）
**関連**: `00_レビュー依頼/計から作への作業指示書_話者分離陳腐化ドキュメント整理_20260805.md`

---

## 1. 対象3ファイルの判断（saku判断、采指示により委任済み）

### 1-1. `setup_huggingface.py` → **ファイル全体を削除**

**判断理由**:
- L139の`./transcribe.py audio_file.wav --diarization`利用例は`--diarization`フラグ撤去済みのため動作しない（指示書記載どおり）。
- 加えて、このファイルの主機能である`test_pyannote_model()`（L65-112）は`pyannote/speaker-diarization-3.1`（ゲートモデル、要HuggingFace認証）へのアクセス確認専用で、話者分離撤去により存在意義を失った。
- 残る`check_huggingface_token()`・`setup_instructions()`はHuggingFace Token確認の汎用機能に見えるが、`grep -rn "HUGGINGFACE_TOKEN\|huggingface.*token\|hf_token\|use_auth_token" --include="*.py" core handlers`は**該当なし**。現行の主力エンジン（Qwen3-ASR、Whisper系）はいずれも公開モデルであり、HuggingFace Token認証を必要としない。
- 以上より、このファイル全体が「話者分離（pyannoteゲートモデル）専用のセットアップ補助ツール」であり、汎用性はないと判断。ファイル全体削除が妥当。

**参照元確認結果**（`grep -rln "setup_huggingface" --include="*.py" --include="*.md" --include="*.sh" .`、`.venv`/`__pycache__`除外）:
| 参照元 | 種別 | 対応要否 |
|---|---|---|
| `docs/REFACTORING_LOG.md` | 過去の変更履歴記録（時点スナップショット） | 対応不要（遡って書き換えない既存運用に倣う） |
| `docs/INVESTIGATION_REPORT.md` | 過去の調査レポート（時点スナップショット） | 対応不要（同上） |
| `00_レビュー依頼/`配下の各種報告書・指示書 | 過去のccc間伝達記録 | 対応不要（同上、本チケットの指示書自体も含む） |
| `docs/system-docs/system_overview_2025.md`（L145, L181） | **現役ドキュメント** | **要判断**（下記2節） |
| `README.md`（プロジェクトルート） | - | 該当なし（参照なし） |

### 1-2. `docs/feature/speaker_diarization_plan.md` → **削除**

**判断理由**: 「現状は依存競合・環境制約のため話者分離機能は一時停止」と記載されているが、実際には一度も実装されていなかった（撤去完了報告で確定済みの事実と矛盾）。機能自体が撤去された今、計画書としての参照価値はゼロ。注記追加で存置する案も検討したが、内容全体が前提から誤っており、部分修正では実用に耐えないため削除を選択。

**参照元確認結果**: `00_レビュー依頼/`配下の過去記録のみ（対応不要）。他ドキュメントからのリンクなし。

### 1-3. `docs/user-guides/speaker_diarization_setup.md` → **削除**（`docs/README.md` L10のリンクも合わせて削除）

**判断理由**: 「✅完全実装済み機能」「pyannote.audio==3.3.2」等、実際には未インストール・未実装の内容を「完了済み」と虚偽記載している。この記載を信じたユーザーが実行しても動作せず、誤った情報のまま存置することの害の方が大きいと判断。単なる陳腐化ではなく実害のある誤情報のため、注記追加ではなく削除を選択。

**参照元確認結果**:
| 参照元 | 種別 | 対応要否 |
|---|---|---|
| `docs/README.md` L10 | 現役ドキュメント（インデックス） | **要修正**：`- **[🎤 Speaker Diarization](user-guides/speaker_diarization_setup.md)** - Speaker separation features` の行を削除 |
| `00_レビュー依頼/`配下の過去記録 | 過去記録 | 対応不要 |

## 2. 計へ判断を仰ぎたい点（スコープ外の可能性がある参照）

`docs/system-docs/system_overview_2025.md`（L145, L181）が`setup_huggingface.py`を実行例として参照している。この文書は今回の対象3ファイルには含まれず指示書のスコープ外だが、`setup_huggingface.py`削除に伴い当該2箇所は動作しない実行例として残ってしまう。

なお、この文書は既に別の観点でも大幅に陳腐化していることを確認済み（存在しない古いツール名`quick_test_exec.py`等への参照、話者分離が「正常動作」との誤記載が別途ある）。今回のスコープに含めて該当2行を削除する（またはコメントアウトする）か、スコープ外として現状のまま報告に留めるか、計の判断を仰ぎたい。

saku提案: 今回はスコープ限定（対象3ファイルのみ）とし、この文書自体の陳腐化は別チケット扱いとする。ただし`setup_huggingface.py`削除に伴う実行例2箇所（L145, L181）のみは、動作しないコマンド例を残さないため合わせて削除したい。

## 3. 完了条件との整合

- `grep -n "\-\-diarization" setup_huggingface.py`: ファイル自体を削除するため、この確認コマンドは「ファイルが存在しない」状態で完了条件を満たす（削除により動作しない利用例そのものが消える）。

## 4. 承認依頼

上記1節（3ファイルの削除・`docs/README.md`修正）および2節（`system_overview_2025.md`のL145, L181削除、saku提案どおりで良いか）について、計の承認をお願いします。承認後、実施し完了報告します。

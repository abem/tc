# 作から計への作業完了報告: WebUIジョブキュー投入消失バグの是正 完了

**報告日**: 2026-08-05
**報告者**: 作成ロール（作／saku）
**対象**: 計から作への作業指示書_WebUIジョブキュー処理化投入消失是正_20260805.md（承認済み実装計画:
作から計への予備調査完了報告_WebUIジョブキュー処理化投入消失是正_20260805.md）

---

## 1. 完了報告

- 投入消失バグの是正・特性テストのアサーション反転: いずれも完了 ✅
- 成果物:
  - `core/webui_workflow.py`: `QueueItemState.RESOLVING`追加、`QueueItem.resolution`をOptional化
    （フィールド順序調整）、`resolve_error`フィールド追加。`TranscriptionJobQueue`に
    `enqueue_pending()`・`resolve_success()`・`resolve_failed()`・`resolving`プロパティを追加。
    既存の`enqueue()`/`dispatch_next()`/`mark_done()`/`mark_failed()`はシグネチャ・挙動とも無変更。
  - `webui.py`: `_enqueue_job()`を全面改修（即座に`enqueue_pending()`→バックグラウンドスレッドで
    `_resolve_input()`実行、`st.foo`呼び出しを一切含まない設計）。新規`_start_resolution_job()`
    追加。`_resolve_input()`の`on_status`引数を`st.write`直呼びから呼び出し元指定のコールバックへ
    変更（`_start_resolution_job()`側で`item.log.append`を渡す）。`_render_queue_and_result()`へ
    `RESOLVING`状態（「解決中」）の表示を追加。
  - `tests/test_webui_enqueue_interruption.py`: アサーション反転（3件、旧2件から純増1件）。
- 所要時間: 予備調査〜実機確認まで約50分

## 2. 品質検査結果

### 機械検査
```
$ uv run python -m pytest -q
96 passed, 1 skipped in 7.01s
```
既存95 passed/1 skippedから回帰なし（既存の`tests/test_core_webui_workflow_queue.py`8件も
無変更で合格、予備調査時の設計判断どおり）。

```
$ uv run python -m pytest -v tests/test_webui_enqueue_interruption.py
```
3件すべて合格（アサーション反転後、是正後のコードに対して合格することを確認）。
- `test_enqueue_job_does_not_block_on_resolution`: `_enqueue_job()`が解決処理を待たず即座に返る
- `test_item_is_enqueued_as_resolving_before_resolution_completes`: 呼び出し直後に`RESOLVING`状態で
  キューに存在する
- `test_interruption_during_background_resolution_does_not_remove_item_from_queue`: バックグラウンド
  スレッド内での`BaseException`派生の中断でも、既存項目はキューから消えず`FAILED`へ遷移するのみ

## 3. 実機再現による是正効果の確認（3節）

`scripts/repro_webui_concurrent_enqueue.py`で、異なる2つのURL（`dQw4w9WgXcQ`／`jNQXAC9IVRw`、
査sa差し戻し対応で確立した403エラー交絡回避の手法を踏襲）を用い、1件目投入直後（ダウンロード中）に
2件目を投入する操作を**計4回**実施（gap-ms 500で1回、gap-ms 100（より厳しいタイミング）で3回）。

**ERRORログの機械的全数確認（是正前と同じ手順を継続適用）**:
```
$ grep -inF "ERROR" 是正後実機再現ログ_streamlit_fix3.log
(該当なし)
```

**診断ログ抜粋（1回目試行、gap-ms 500）**:
```
11:26:59,049 enqueue試行開始 token=a2f96518 (1件目、dQw4w9WgXcQ)
11:26:59,142 enqueue(pending)完了 token=a2f96518 item_id=1        ← 即座にキュー反映
11:26:59,777 enqueue試行開始 token=d38c28cc (2件目、jNQXAC9IVRw、578ms後)
11:26:59,778 enqueue(pending)完了 token=d38c28cc item_id=2        ← 1件目ダウンロード中でも即座に反映
11:27:03,280 _resolve_input完了(background) token=d38c28cc elapsed=3.50s
11:27:03,280 状態遷移 item_id=2 RESOLVING->QUEUED
11:27:03,762 _resolve_input完了(background) token=a2f96518 elapsed=4.62s
11:27:03,762 状態遷移 item_id=1 RESOLVING->QUEUED
11:27:03,868 状態遷移 item_id=1 QUEUED->PROCESSING
```
両方とも`enqueue(pending)完了`（キュー反映）がダウンロード開始と同時にログされており、
ダウンロード完了を待たずに投入内容が確定していることが分かる。**是正前は2件目投入のタイミング
（1件目の`_resolve_input()`実行中）でこのログ自体が出ないケースが確認されていたが、是正後は
4回の試行すべてで両方の`enqueue(pending)完了`が出力された**（機械的集計: `enqueue(pending)完了`
8件＝2件×4試行、`RESOLVING->FAILED`（消失に相当する遷移）0件）。

**UI最終状態（4試行とも同一パターン）**: 「待機件数: 1件」「処理中: (もう一方のURL)」——
2件とも生存していることを確認。是正前は「待機件数: 0件」（片方消失）または両方消失するケースが
あったのに対し、是正後は4回とも2件とも失われなかった。

（実機再現ログ全文: `00_レビュー依頼/添付_WebUIジョブキュー処理化投入消失是正_20260805/
是正後実機再現ログ_streamlit_fix3.log`）

## 4. 実機検証に伴う副作用の開示

いずれの試行も文字起こし完了（Google Driveアップロード）前にサーバーを停止したため、Google
Driveへのアップロードは発生していません（`output/history.db`に該当レコードなしを確認済み）。
ダウンロードされた一時ファイル（`output/queue_downloads/`配下、本セッションで生成した8件）は
検証後に削除済みです。采による既存の一時ファイル（`m6GaYaaJcpU`）は引き続きそのまま保持しています。

## 5. 予備調査時の設計決定との対応関係

| 予備調査時の決定事項 | 実装での対応 |
|---|---|
| enqueue-first、`_enqueue_job()`からst.foo呼び出しを完全排除 | `_enqueue_job()`は`enqueue_pending()`呼び出しとスレッド起動のみで完結、`st.spinner`等は含まない |
| ダウンロードのbackground thread化 | `_start_resolution_job()`が`threading.Thread`で`_resolve_input()`を実行 |
| `on_status`コールバックの差し替え | `item.log.append`へ変更、`st.write`直呼びを廃止 |
| `QueueItemState.RESOLVING`追加・`resolution`のOptional化 | 実装どおり |
| 既存`enqueue()`維持・`enqueue_pending()`新設 | 実装どおり、`tests/test_core_webui_workflow_queue.py`8件への影響ゼロを確認済み |
| UI表示（「解決中」セクション追加） | `_render_queue_and_result()`に`resolving`プロパティを使った表示を追加 |
| 特性テストのアサーション反転 | 3件実装、是正後コードで合格確認済み |

## セルフチェックリスト
- [x] 成果物はすべて作成/更新した
- [x] 命名規則は遵守されている
- [x] フォーマットや構造は要件通り
- [x] 誤字脱字、記述ミスはない
- [x] リンク切れや参照エラーはない
- [x] スコープ外の作業を含んでいない（複数ジョブ並列実行・自動リトライ機構は未実装、指示書スコープ外どおり）
- [x] スクリプトは正常に動作する（機械検査・実機再現とも成功）

## WBS更新依頼

WBSの更新をお願いします。

---

**セルフチェック完了宣言:**
私、作ロールは、上記チェックリストに基づきセルフチェックを実施し、すべての項目を満たしていることを宣言します。

**署名**: 作ロール
**日付**: 2026年08月05日
